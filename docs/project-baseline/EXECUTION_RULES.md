# 执行规则

1. 每阶段只解决一个主要未知量。
2. 每个优化必须有优化前、优化后和无关变量说明。
3. 每个命令必须能够回滚或由不可变基线重建。
4. 外部数据在边界验证，内部状态依赖类型和不变量。
5. 不用长超时、重试循环或 fallback 掩盖内部并发错误。
6. Debug 结果不能当性能结论。
7. Root 观察到的事件不能直接推导普通应用可获取。
8. 不满足退出门槛时，记录阻塞，不用下一阶段代码补偿。
